apiVersion: v1
kind: Service
metadata:
  name: {{ include "devops-webapp-python.fullname" . }}
  labels:
    {{- include "devops-webapp-python.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "devops-webapp-python.selectorLabels" . | nindent 4 }}
