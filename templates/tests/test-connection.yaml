apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "devops-webapp-python.fullname" . }}-test-connection"
  labels:
    {{- include "devops-webapp-python.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "devops-webapp-python.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
