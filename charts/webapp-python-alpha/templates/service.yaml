apiVersion: v1
kind: Service
metadata:
  name: {{ include "webapp-python-one.fullname" . }}
  labels:
    {{- include "webapp-python-one.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "webapp-python-one.selectorLabels" . | nindent 4 }}
